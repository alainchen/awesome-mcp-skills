{
  "name": "awesome-mcp-skills",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:readme": "node scripts/generate-readme.mjs",
    "generate:readme:check": "node scripts/generate-readme.mjs --check",
    "validate:data": "node scripts/validate-data.mjs",
    "test": "node --test",
    "check": "npm test && npm run validate:data && npm run generate:readme:check"
  }
}
